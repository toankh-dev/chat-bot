# ============================================================================
# KASS Chatbot - Environment Configuration Example
# ============================================================================
#
# IMPORTANT: This is configured for LOCAL DEVELOPMENT with Gemini (no AWS required)
#
# For Production with AWS Bedrock:
# - Change LLM_PROVIDER=bedrock
# - Change EMBEDDING_MODEL=amazon.titan-embed-text-v1
# - Configure AWS credentials
# ============================================================================

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
ENVIRONMENT=development
APP_NAME=kass-chatbot
APP_VERSION=1.0.0
DEBUG=true
LOG_LEVEL=INFO

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chatbot
DB_USER=chatbot
DB_PASSWORD=your-database-password

# ============================================================================
# AUTHENTICATION
# ============================================================================
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================================================
# AWS CONFIGURATION (Optional - Only needed for S3 file storage or Bedrock)
# ============================================================================
# For LOCAL DEVELOPMENT: You can skip AWS if using local file storage
# For PRODUCTION: Configure these for S3 storage and/or Bedrock
#
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key

# ============================================================================
# LLM CONFIGURATION
# ============================================================================
# Provider: gemini (local development) or bedrock (production with AWS)
LLM_PROVIDER=gemini

# ----------------------
# Google Gemini API (Recommended for Local Development)
# ----------------------
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-2.5-flash
# Available models:
# - gemini-2.5-flash (Fastest, best for development)
# - gemini-1.5-flash (Fast, cost-effective)
# - gemini-1.5-pro (Most capable, slower)
# - gemini-2.0-flash-exp (Experimental)

# ----------------------
# AWS Bedrock (Optional - Only for Production)
# ----------------------
# Uncomment these if LLM_PROVIDER=bedrock
# BEDROCK_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0
# BEDROCK_REGION=us-east-1
# BEDROCK_MAX_TOKENS=4096
# BEDROCK_TEMPERATURE=0.7

# ============================================================================
# STORAGE CONFIGURATION
# ============================================================================
# File Storage Provider: local (development) or s3 (production)
FILE_STORAGE_PROVIDER=local

# ----------------------
# Local File Storage (Recommended for Local Development)
# ----------------------
LOCAL_STORAGE_PATH=./local_storage
# Documents will be stored in: ./local_storage/documents/{domain}/{user_id}/

# ----------------------
# S3 Storage (Optional - Only for Production)
# ----------------------
# Uncomment if FILE_STORAGE_PROVIDER=s3
# S3_BUCKET_NAME=your-s3-bucket-name

# ============================================================================
# KNOWLEDGE BASE CONFIGURATION
# ============================================================================

# ----------------------
# Vector Store Provider
# ----------------------
# Options: chromadb (local development) or s3 (production)
VECTOR_STORE_PROVIDER=chromadb

# ----------------------
# ChromaDB Configuration (Recommended for Local Development)
# ----------------------
CHROMADB_HOST=localhost
CHROMADB_PORT=8000
CHROMADB_PERSIST_DIRECTORY=./chroma_db
# Note: ChromaDB will auto-create the directory if it doesn't exist
# Run ChromaDB: docker run -p 8000:8000 chromadb/chroma
# Or use embedded mode (no docker needed)

# ----------------------
# Knowledge Base IDs (Domain-specific collections)
# ----------------------
KNOWLEDGE_BASE_HEALTHCARE_ID=kb_healthcare
KNOWLEDGE_BASE_FINANCE_ID=kb_finance
KNOWLEDGE_BASE_GENERAL_ID=kb_general

# ----------------------
# Document Processing Configuration
# ----------------------
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
MAX_CHUNKS_PER_DOCUMENT=500

# ----------------------
# Embedding Configuration
# ----------------------
# IMPORTANT: Embedding provider automatically matches LLM_PROVIDER
#
# For Gemini (current setup):
EMBEDDING_MODEL=models/embedding-001
EMBEDDING_DIMENSION=768
# Available Gemini models:
# - models/embedding-001 (768-dim, recommended)
# - models/text-embedding-004 (768-dim, latest)
#
# For Bedrock (if switching to production):
# EMBEDDING_MODEL=amazon.titan-embed-text-v1
# EMBEDDING_DIMENSION=1536
# Available Bedrock models:
# - amazon.titan-embed-text-v1 (1536-dim)
# - amazon.titan-embed-text-v2:0 (1024-dim, cost-optimized)

# ============================================================================
# API CONFIGURATION
# ============================================================================
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
